pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.{ts,tsx,js,jsx}"
      run: bunx oxlint {staged_files}
    - name: typecheck
      glob: "*.{ts,tsx}"
      run: bun run typecheck
